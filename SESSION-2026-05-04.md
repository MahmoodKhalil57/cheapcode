# cheapcode session 2026-05-04 — M16 → M17 → M18 Phase 1 → M19

For the next session that picks this up. Honest log, not marketing.

## What shipped (verified)

**M16 — OAuth multi-account in fork** (cheapcode-opencode dev branch)
- 5-layer fix: UI dialog → v2 SDK type → SDK buildClientParams mapping → server route handler → ProviderAuth.callback save target.
- Server `Provider.ListResult.credentials` exposes aliased entries with canonical `providerID` link + longest-prefix fallback for legacy entries.
- Settings → Providers renders one row per credential with own Disconnect; "+ Add another" gated to credential-bearing sources only.
- Settings → Models renders one section per credential with independent toggle state.
- Fork commits: `85cd4d7c1`, `28d410423`, `9d38280c1`.

**M17 — auto-router stack** (cheapcode main, 7 modules, 71 unit tests, 102ms)
- `cooldown.ts`, `credential-pool.ts`, `dispatch-with-pool.ts` — Phase A.
- `temporal-anchor.ts`, `sycophancy-probe.ts` — Phase B.1, B.2 (sycophancy detection rate ≥80% of synthetic traps gated in test suite).
- `quota-tracker.ts` — Phase C (header parse + ms+usd budget + JSONL telemetry with 10MB rotation).
- `orchestrate.ts` — composes A+B+C in one entry; safety properties (probe failures can't block primary, telemetry can't block dispatch).
- Live benchmark wiring at `script/m17-benchmark.ts` + `script/m17-live-dispatch.ts`. Verified end-to-end against operator's actual auth.json.

**M18 Phase 1 — Eve-curated canon** (cheapcode main)
- `script/m18-discover-canon.ts` (~270 LoC) — fetch-only, no LLM dispatch. Discovery is HTTP-grounded, not training-recall paraphrased. Per atom 0007.
- 17/19 candidates committed across 8 dimensions (software-architecture, api-dx, ui-visual, accessibility, ux-research, ai-ml-product, llm-failure-research, policy-governance).
- All entries `mizan_grade: "daif"` until Phase 2 verifies.
- Discovery pipeline caught a Phase-2-flag-worthy anomaly: TicToc seed (arxiv 2503.02391) actually points to a different paper. Logged in `plan/canon/STATUS.md`. Anti-fab discipline working as designed.

**M19 — research-first canon, countermeasure experiments, runtime implementation, scorecard** (cheapcode main)
- Phase 1 expanded canon to 52 fetched candidates across 8 dimensions and added `bun script/m18-discover-canon.ts --validate`.
- Phase 1 corrected the bad TicToc seed to arXiv `2510.23853` and removed a poisoned inclusive-design URL that fetched gambling spam.
- Phase 2 wrote six 10-prompt benchmark files plus synthetic and live ChatGPT OAuth receipts. Live marker-scored deltas were positive for temporal-blindness, fact-vs-belief, and cultural-homogenization; null for sycophancy, reversal-curse, and RLHF-bias.
- Phase 3 wrote `plan/M19-AGENT-DESIGN.md` (3267 words) with telos, pipeline, skills, tools, mizan integration, energy transformation, confidence accounting, and anti-claims.
- Phase 4 shipped `canon-loader.ts`, `canon-injector.ts`, `claim-shape.ts`, orchestrate `canonInjection` / `claimShapeVerify` flags, M17 live-dispatch env wiring, and `script/m19-scorecard.ts`. Tests: 388 passing.
- Phase 5 scorecard receipt: `plan/receipts/m19-scorecard-final-2026-05-04T21-30-57Z.json`. Internal threshold MET by local scorer: 3/7 axes improved, 0 meaningful regressions. External claim remains blocked until operator spot-checks 20 outputs at >=80% canon relevance.

## What is NOT measured (predicted, not validated)

- **"Smarter / faster / cheaper than gpt-5.5"** — gated behind `plan/receipts/m17-paired-benchmark-*.json` showing ≥7/10 wins on cheaper-OR-equal-OR-better axis. **First real receipt produced** at `plan/receipts/m17-first-real-receipt-2026-05-04.json`: 4/10 wins, identical 4/4 PASS rate per arm, cheapcode 13% faster wall-clock (152.8s vs 175.1s). Headline claim NOT earned. Both arms used the same model (consumer-Plus OAuth → codex/responses) so the comparison reflects orchestrate overhead, which is *negative* (cheapcode faster).
- **"Big step toward better general agent" (M18/M19 headline)** — M19 internal local-scorecard threshold is met (3/7 axes improved, 0 regressed) at `plan/receipts/m19-scorecard-final-2026-05-04T21-30-57Z.json`, but external claim is still NOT earned until operator spot-checks 20 outputs at >=80% canon relevance. `canonInjection` and `claimShapeVerify` remain opt-in flags.
- **Sycophancy countermeasure effect** — Phase 2 live marker-scored prompts showed a null result (baseline 10/10 failures, countermeasure 10/10 failures). Keep sycophancy changes opt-in/redesign until better evidence.
- **Temporal-anchor prompting effect size** — Phase 2 live marker-scored prompts showed a positive result (baseline 10/10 failures, countermeasure 3/10 failures), but Phase 5 scorecard is local-scored and still needs operator review.

## What's blocked (genuinely, not lazily)

1. ~~M17 paired-benchmark first receipt~~ — **UNBLOCKED**. Wired the consumer-Plus OAuth → `chatgpt.com/backend-api/codex/responses` dispatch path (`src/chatgpt-oauth-fetch.ts` + direct SSE parse) so OpenAI-only auth.json works end-to-end without api-keys. First real receipt produced 2026-05-04 20:47 UTC.
2. M18-style 4-LLM cross-witness verification is still not implemented. M19 intentionally ships runtime loading from candidates but leaves canon entries `daif` until verification promotes them.
3. Operator 20-output canon-relevance spot-check is still required before any external "better general agent" claim.

## Where to pick up

Read in this order:
1. `SESSION-2026-05-04.md` (this file) — 60-second context.
2. `plan/M17-DISPATCH-CONTRACT.md` — auto-router contract; integration into `cheapcode-tiers.ts createCheapcodeProvider` is the remaining surface work for M17 to be runtime-default.
3. `plan/M19-IN-PROGRESS.md` — phase-by-phase receipts and anti-fab self-checks.
4. `plan/M19-AGENT-DESIGN.md` — implemented design target.
5. `plan/receipts/m19-scorecard-final-2026-05-04T21-30-57Z.json` — Phase 5 verdict.

## Pride checklist (what I want preserved)

- **Anti-fab gate baked into output**, not just discipline. The benchmark prints `→ headline-claim threshold NOT met (need ≥7/10, got X/10). NO 'smarter/faster/cheaper' claim.` even on its own dry-runs. The next agent can't accidentally publish unearned claims.
- **Atomic schemas**. Cooldown.json has `version: 1`. Receipts have `version: 1`. Canon candidates have `version: 1`. Schema evolution is forward-compatible.
- **Fail-soft compositions**. Telemetry can't block dispatch. Probe failure can't block primary. Daftar shell-out fail-softs to `[]`. Cooldown tolerates corrupt JSON.
- **Discovery is web-fetch grounded, not LLM-paraphrased.** Per operator's framing — Eve fetches, doesn't recall. The TicToc anomaly proves the discipline works.
- **Per-credential UI parity**. Both Providers AND Models pages render one row per auth.json key, so multi-account control is actually usable, not theoretical.

## What I'd do next if I had more context

1. Run the operator spot-check: 20 canon-on outputs, score whether injected canon was relevant and honored; require >=80% before any external claim.
2. Write `script/m18-verify-canon.ts` / M19 verification pass to promote `daif` entries to `hasan` or `sahih` with cross-witness receipts.
3. Redesign the sycophancy, reversal-curse, and RLHF-bias countermeasures because Phase 2 live receipts were null.
4. Replace the local scorecard scorer with a stronger semantic/live scorer before treating Phase 5 as product-quality evidence.

## Memory pointers (for the next session)

- `~/.claude/projects/-home-mk-apps-aapi/memory/MEMORY.md` — index
- `project_m17_dispatch_handoff.md`, `project_m18_eve_canon.md` — current handoff state
- `~/.claude/CLAUDE.md` — substrate-suite + Adam-Eve frame canonical
