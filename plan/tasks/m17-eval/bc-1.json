{
  "id": "bc-1",
  "shape": "bounded-code",
  "prompt": "Write a TypeScript function `chunk<T>(arr: T[], size: number): T[][]` that splits an array into chunks of `size`. The last chunk may be shorter. Throw RangeError if size <= 0. No external dependencies.",
  "correctness_check": "automated",
  "expected_keywords": ["chunk", "RangeError", "size <= 0"],
  "forbidden_keywords": ["lodash", "import"],
  "notes": "Classic bounded-code; tests handling of edge case + idiomatic TS."
}
