#!/usr/bin/env bash
HERE="$(cd "$(dirname "$0")/.." && pwd)"
PYTHONPATH="$HERE/src${PYTHONPATH:+:$PYTHONPATH}" exec python3 -c \
  "from mizan.cli import mcp_server_main; raise SystemExit(mcp_server_main())" "$@"
